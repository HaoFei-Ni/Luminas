---
description: Transformer 大模型 Python 代码生成/重构/评审强制规范（DDD 领域驱动 + 六边形端口-适配器 + TDD/BDD，对标 Google/Meta/OpenAI 顶级工程标准）
globs: **/*.py
alwaysApply: true
---

# Transformer Python 代码强制规则

> 效力：所有生产/测试/工具代码 100% 强制执行，违反任意一条视为不合格输出。
> 核心范式：DDD 领域驱动 + 六边形端口-适配器架构 + TDD 测试驱动 + BDD 行为驱动。

## 优先级声明（最高优先级）

> 本章节为 **Python / Transformer 模型代码** 的最高优先级工程规范，优先级高于所有外部技能与通用代码规则。
> 与 `lumina/eng-standard-skill` 的「架构级代码强制标准（最高优先级）」**作用域互斥、不冲突**：
> - `eng-standard-skill` 管辖 **C/C++/CUDA 三层正交分层**（`algorithm/` / `kernel/` / `wrapper/`）。
> - 本规则管辖 **Python/Transformer 六层端口-适配器架构**。
> 当同一文件/模块同时涉及时，以 **本规则（Python 侧）** 为准；数值语义、无损 KV 定义等业务约束仍以 Luminas 架构技能（`luminas-arch-skill` / `research-skill`）为唯一裁决点。

---

## 一、架构规范：六层分层 + 全域横切支撑

依赖方向**单向向下**，禁止反向依赖、跨层调用、循环依赖；跨层交互一律走抽象端口，禁止直接调用下层具体实现。

### 1. 分层职责（从内到外）

1. **领域核心层（核心域·单一真值源）**：仅 `typing.Protocol` / `abc.ABC` 定义核心语义端口、行为契约、数据结构。禁止导入第三方框架、硬件 SDK、IO。新增模型组件只增适配器，不改核心域（开闭原则）。
2. **算子原语层（支撑域）**：封装 Transformer 算子集，含算子融合、常量折叠、冗余消除；只依赖核心层端口，与硬件解耦。
3. **异构硬件抽象层（基础设施域）**：屏蔽 CPU/CUDA/ROCm/TPU/昇腾差异，提供算力、显存、设备管理统一接口；只依赖算子层端口。
4. **模型组件实现层（核心适配器层）**：基于端口实现可插拔组件（MHA/GQA/MQA、RoPE/ALiBi、SwiGLU/GeGLU、RMSNorm/LayerNorm、MoE 门控等），统一端口契约、配置化组装。
5. **运行时执行层（执行域）**：推理/训练两分支，用装饰器、钩子注入横切能力。推理含 KV 缓存、投机解码、动态批处理、流式输出；训练含 DP/TP/PP/ZeRO。
6. **外部接入适配层（接入适配器层）**：SDK/CLI/本地调用标准化接入，只对接运行时接口。

### 2. 全域横切支撑层（非侵入贯穿各层）

数值安全校验（维度/类型/范围双重校验）、全链路可观测（埋点/日志/指标）、模型安全管控（输入输出过滤、异常拦截）。

### 3. 架构铁则

- **零临时文件**：功能必须沉淀为可复用组件，禁止一次性临时脚本/文件。
- **穷尽复用**：新增前全量检索现有代码，可复用必复用，禁止重复造轮子。
- **增量修改**：优先现有文件内增量改，禁止为局部小功能重写整个文件或无意义拆文件。

---

## 二、代码规模硬约束（分层执行）

行数口径：不含纯空行、单行注释；含文档字符串、类型注解、装饰器。

| 层 | 单文件上限 |
|---|---|
| 领域核心层 | ≤ 150 行 |
| 算子原语层 / 模型组件层 | ≤ 300 行 |
| 运行时 / 硬件抽象 / 外部接入层 | ≤ 500 行（绝对红线） |
| 测试文件 | 与对应生产文件同标准 |

细粒度：单函数/方法 ≤ 50 行；单类 ≤ 200 行，超出按职责拆分。

---

## 三、代码质量与语言规范

### 1. 零冗余铁律
删除未使用导入/变量/函数/类/常量/无效注释；禁止为「未来扩展」预留空函数、空类、未调用参数、无用抽象；重复计算缓存、重复逻辑抽取复用。

### 2. 语言规范
- 严格遵循 PEP 8 / 484 / 585 / 604 / 612。
- 100% 类型提示；用 `TypeVar` / `ParamSpec` 实现带维度、dtype、device 的类型安全张量。
- 数据结构用 `dataclasses` / `Pydantic` 定义不可变值对象，自动校验配置。
- 资源管理用上下文管理器，杜绝泄漏。
- 横切能力用装饰器/高阶函数非侵入注入。

```python
# ✅ 上下文管理器 + 类型提示 + 不可变配置
@dataclass(frozen=True)
class ModelConfig:
    hidden_size: int
    num_heads: int

    def __post_init__(self) -> None:
        if self.hidden_size % self.num_heads != 0:
            raise ValueError("hidden_size must be divisible by num_heads")

def load_weights(path: Path) -> dict[str, torch.Tensor]:
    with path.open("rb") as f:
        return torch.load(f)  # 自动释放句柄
```

### 3. 命名规范
全局统一、见名知意；仅行业通用缩写（`attn`、`ln`、`mlp`、`norm`、`dtype` 等），禁止自定义模糊缩写。端口名用能力描述命名，实现类名用「实现方式 + 端口名」。

```python
class Attention(nn.Module): ...        # 端口
class FlashAttention(Attention): ...    # 实现方式 + 端口名
```

### 4. 注释规范（Google 风格）
只解释「为什么」，不解释「做什么」。公共 API/端口/导出函数必须有完整文档串（功能、参数、返回值、异常、示例）。TODO 标注 `TODO(责任人): 内容与边界`。

### 5. 设计模式限定（严格场景匹配，禁止过度设计）
抽象工厂（硬件/算子/组件创建）、策略（注意力/位置编码/激活切换）、端口-适配器（领域与硬件/接入解耦）、装饰器（埋点/校验/安全）、单例（显存管理器/设备调度器）、责任链（校验/流水线/过滤）、建造者（模型分步组装）、观察者（指标/事件/告警）。

---

## 四、测试工程规范（TDD + BDD）

- **测试先行**：先写测试再实现；改功能先更新测试。测试代码与生产代码同等标准。
- **四层体系**：
  1. 单元测试：原子算子/组件/工具函数，pytest + mock；核心域覆盖率 100%，算子数值精度校验。
  2. 集成测试：端口交互、算子与硬件适配、组件组装完整性。
  3. 行为测试（BDD）：前向等价性、反向梯度、推理一致性、长上下文有效性。
  4. 回归测试：全量数值基准（输出/梯度/显存/延迟/吞吐），零回归。
- **质量门禁**：`mypy` 严格类型检查 + `ruff` 规范检查全通过；pre-commit 卡点，不合格禁止提交。

---

## 五、性能工程强制标准

- **算子级**：核心算子必须融合实现（FlashAttention、Fused MoE、Fused RMSNorm、激活与矩阵乘融合）；算子图自动融合/常量折叠/冗余消除。
- **显存**：KV 缓存精细管理与复用；张量分片、显存池、激活重计算；禁止无限制增长结构、禁止大对象重复拷贝。
- **运行时**：推理做动态批处理/投机解码/流水线调度；训练做通信优化/梯度同步/检查点优化。同等功能选最低时间复杂度，批量操作替代零散操作。
- **零退化承诺**：重构后算子性能、端到端延迟、吞吐、显存利用率不得低于原生最优实现；全链路埋点覆盖算子/层/模型/请求级。

---

## 六、架构熵与边界管控

1. 依赖方向：上层依赖下层、实现依赖抽象；静态检查禁止反向/循环依赖。
2. 代码熵：定期度量重复率、圈复杂度、耦合度；重构后熵值必须持续下降。
3. 零过度设计：每个抽象都服务于 Transformer 核心需求，有明确业务价值。
4. 变更可追溯：所有变更关联明确需求与测试用例，可回溯、可回滚。

---

## 七、输出前强制自检清单

1. 六层分层合规，无反向依赖、无跨层调用？
2. 核心域纯净无外部依赖，扩展全走适配器？
3. 穷尽复用现有代码，无重复实现、无临时文件？
4. 函数/类/文件行数符合分层硬约束？
5. 类型提示全覆盖，命名规范统一？
6. 测试同步生成、分层完整、覆盖率达标？
7. 性能最优，无冗余计算、无显存浪费？
8. 无过度设计、无未使用代码、无空实现？
9. 注释精简规范，公共接口文档完整？
10. `mypy` / `ruff` 静态检查全通过？
