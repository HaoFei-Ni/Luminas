# lumina/theory

Architecture-agnostic method notes. Native Luminas science lives here; kernels only implement it.

| Module | Role |
|---|---|
| [state-cache/](state-cache/) | 架构无关状态缓存压缩：闭合框架、推导、符号核验 |

投稿排版不放这里，见日后的 `lumina/paper/`。实验协议不放这里，见 `lumina/research/`。
