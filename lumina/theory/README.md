# lumina/theory

方法与闭合理论笔记。内核只实现经裁决后的命题；本目录不授予产品「无损」声明。

| 模块 | 角色 |
|---|---|
| [state-cache/](state-cache/) | **权威**：表征坍缩统一理论（`framework.tex` / `.pdf` / `.md` + F1–F7） |
| `kv-cache-*.md` 等 | 历史笔记；不得覆盖 `state-cache/` 结论 |

投稿排版 → 日后的 `lumina/paper/`。实验协议 → `lumina/research/`。产品路径三级无损门 → `lumina-res-skill`。
