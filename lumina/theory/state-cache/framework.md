# 架构无关的状态缓存压缩：定律级闭合数学框架

> 工作稿。权威排版见 [framework.tex](framework.tex)。未写入证明的小节保持「待证」，禁止把待证命题写进论文结论。

## 0. 范围

- 输入：任意骨干产生的状态缓存 \(S\)（KV、SSM 状态、事件状态）。
- 输出：压缩表示 \(C\) 与重构 \(\widehat{S}\)。
- 不在此证明：具体模型的 PPL / 任务门（那是 `research-skill`）。

## P0. 定义

**待写入。** 给出状态缓存作为张量（或分块流）的最小公理：有限、可索引、与骨干解耦。写出 \(\mathrm{Enc},\mathrm{Dec}\) 的定义域与值域。

## P1. 重构

**待证。** 陈述 \(\widehat{S} = \mathrm{Dec}(\mathrm{Enc}(S))\) 的误差界，并标清是恒等、可逆变换，还是带 2-ulp 余项的近似。

当前实现是恒等映射，只用于占住 ABI。它不构成 P1 的证明。

## P2. 数值门

**待写入闭合。** 证明或反驳：P1 的误差界 \(\Rightarrow\) 2-ulp 门（\(\ge 99.9\%\) 元，无 NaN/Inf）。核验脚本与 `test_luma_kv` 必须引用本编号。

## P3. 下游等价

**待证。** 在何种算子类（softmax 注意力、线性扫描、脉冲累积）下，\(\widehat{S}\) 与 \(S\) 给出相同（或 2-ulp 内）的下一步状态。这是「架构无关」能否成立的核心，未证之前不得称定律闭合。

## P4. 复杂度

**待写入。** 时间与 \(\lvert C\rvert\) 对 \(T,H,d\) 的式子。恒等实现的 \(\rho = 1\)，不得当作方法收益。

## P5. 闭合与失效

**待写入。** 列出无损区边界：非有限输入、溢出、长度/动态范围。与实验室日志中的失败 run 对齐。

## 全链路推导（展开位）

按 P0→P5 顺序写。每步：假设 → 公式 → 结论编号。不要跳号。数值算例放到 [verify/](verify/)，不要把 notebook 当证明。
